on:
  workflow_dispatch:
  schedule:
    - cron: '40 * * * *'

name: Timed GitHub Action

jobs:
  timed_action:
    name: Job for timed action
    runs-on: ubuntu-latest
    
    container:
      image: rocker/tidyverse:4.2
    permissions:
      contents: write
    
    steps:
      - name: Hello
        run: |
          Rscript -e "message('Hello from R')"
